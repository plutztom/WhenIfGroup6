import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'when-if-root',
    templateUrl: 'app.component.html',
    styleUrls: ['app.component.scss']
})

export class AppComponent {
}
